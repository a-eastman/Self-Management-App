///Overall stats based on the user's history
///Page relies heavily on DB queries
///@author Martin Price
///date April 18, 2019
/*
import 'package:flutter/material.dart';
import 'database.dart';
import 'bubbles.dart';
import 'themes.dart';
import 'dart:async';

class StatsWidget extends StatefulWidget{
  BubbleTheme theme;
  StatsWidget(BubbleTheme theme){
    this.theme = theme;
  }
  StatsWdidgetState createState() => StatsWidgetState(this.theme);
}

///Builds the stats page
class StatsWidgetState extends State<StatsWidget>{
  BubbleTheme theme;
  BubblesList bList;
  
  StatsWdidgetState(BubbleTheme theme){
    this.theme = theme;
  }

  @override
  Widget build(BuildContext context){
    double screenHeight = MediaQuery.of(context).size.height;
    double screenWidth = MediaQuery.of(context).size.width;
  }
}*/